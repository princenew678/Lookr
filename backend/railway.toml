# Railway deployment configuration for Lookr Backend
# This file tells Railway how to build and run the backend

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node src/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "lookr-backend"
